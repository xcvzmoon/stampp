# Shared runtime identity for every Stampp process.
# @currentEnv=$APP_ENV
# @defaultSensitive=false
# ---

# @type=enum(development, test, production)
APP_ENV=development

# Public origin of the app (same origin behind reverse proxy).
# @required @type=url @public
PUBLIC_APP_URL=http://localhost:3000
